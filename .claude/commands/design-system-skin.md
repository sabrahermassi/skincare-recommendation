I've attached reference screenshots of UI I want you to draw design direction from for this skincare recommendation app.

I already have a chosen color palette I want to keep — do not propose new colors. Instead, analyze how color is used _structurally_ in the screenshots (which elements are background vs. surface/card, which get the primary accent, which get a secondary/highlight color, where text contrast is high vs. muted), then map my palette into those same roles.

My palette (Milky pastel):

- Baby pink #F7D9DA
- Mint whisper #D9EDE3
- Lilac milk #E3DCF0
- Soft peach #FBE4D8
- Cloud white #FDFBF9

Using the ui-ux-pro-max skill's style, palette, and typography database, propose a full design system for this app based on the reference screenshots and my palette above:

1. **Typography** — a font pairing (heading + body) that fits the aesthetic in the screenshots, adapted to a warm, relaxing, skincare-appropriate feel.
2. **Spacing and layout rules** — whitespace density, grid/padding conventions, corner radius conventions for cards and buttons.
3. **Component styles** — how buttons, cards, tags, and form inputs should look, consistent with the reference screenshots.
4. **Design tokens** — express all of the above as a reusable theme file or CSS variables (matching how this project is set up), so every screen can pull from one consistent source instead of hardcoded styles per screen.

Do not implement anything yet and do not modify any files. Don't build any screens. Just propose the design system as a summary I can review and approve first.
