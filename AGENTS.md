# Expo HAS CHANGED

Before writing code that touches an Expo API, config field, or SDK
package directly — not for changes that only touch app/business logic —
check the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/
first. Training knowledge of Expo is frequently out of date.
