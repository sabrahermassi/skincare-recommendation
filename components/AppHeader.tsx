/**
 * The side gutter of a tab's top row and the content below it (26pt, the
 * scanner's camera card), so nothing in the header hangs off the edge of the
 * screen. The heart masthead this file used to draw is gone: Browse, its last
 * user, now shows a back arrow to Home instead.
 */
export const HEADER_GUTTER = 26;
